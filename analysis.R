#view the first few rows of mtcars datset 
head(mtcars)

#calculate the average mpg among all cars and save it as variable 
mean_mpg <- mean(mtcars$mpg)

#print the mean mpg
mean_mpg

#this is a random comment to practice pushing to github